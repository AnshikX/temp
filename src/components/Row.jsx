import { React } from "react";
import { Row } from "react-bootstrap";
import { Col } from "react-bootstrap";
const Row = ({ style }) => {
  return (
    <Row>
      <Col />
      <Col />
    </Row>
  );
};
export default Row;
